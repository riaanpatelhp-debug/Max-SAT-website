{
  "name": "mpl-api-reference",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reference API layer for Max Potential Learning — OWASP-aligned rate limiting, input validation, and secure key handling. Wire this up when you add real endpoints (e.g. a contact form). It is NOT used by the static site.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "zod": "^3.23.8"
  }
}
